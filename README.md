# Student-management-system
A web-based portal to manage student data efficiently. Includes CRUD operations, a real-time search filter, and a clean UI. Uses browser LocalStorage to keep data saved without a backend
